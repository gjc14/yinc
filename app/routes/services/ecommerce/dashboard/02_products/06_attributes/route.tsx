import type { Route } from './+types/route'

export default function ECAttributes({
	loaderData,
	actionData,
}: Route.ComponentProps) {
	return (
		<div>
			{/* Frontend Code here. */}
			<h1>ECAttributes</h1>
		</div>
	)
}
