import type { Route } from './+types/route'

export default function ECTags({
	loaderData,
	actionData,
}: Route.ComponentProps) {
	return (
		<div>
			{/* Frontend Code here. */}
			<h1>ECTags</h1>
		</div>
	)
}
